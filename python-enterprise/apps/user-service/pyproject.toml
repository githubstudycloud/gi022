[project]
name = "user-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pe-core",
    "pe-config",
    "pe-database",
    "pe-cache",
    "pe-auth",
    "pe-messaging",
    "pe-observability",
]

[tool.uv.sources]
pe-core         = { workspace = true }
pe-config       = { workspace = true }
pe-database     = { workspace = true }
pe-cache        = { workspace = true }
pe-auth         = { workspace = true }
pe-messaging    = { workspace = true }
pe-observability = { workspace = true }
