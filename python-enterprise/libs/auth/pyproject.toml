[project]
name = "pe-auth"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-jose[cryptography]>=3.3",
    "passlib[bcrypt]>=1.7",
    "fastapi>=0.115",
    "pe-core",
]

[tool.uv.sources]
pe-core = { workspace = true }
