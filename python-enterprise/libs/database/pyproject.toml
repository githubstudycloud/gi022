[project]
name = "pe-database"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pe-core",
]

[tool.uv.sources]
pe-core = { workspace = true }
